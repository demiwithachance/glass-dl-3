# Optional sprite packs

Place your own sprite image here as `loader.gif`, `loader.webp`, or `loader.png`.

Glass DL automatically uses the first matching file as its download animation. If none exists, it uses the built-in CSS loader. Sprite assets are intentionally not bundled or hardcoded.
